# SD Import 2.1.2

- Replaces the video-only `tmp-YYYY-MM-DD-videos` destination folder with clear dated shoot folders.
- Uses the shoot name for both photo and video folders in Separate Folders imports.
- Prevents folder collisions when photo and video destinations point to the same root by using `-Photos` and `-Video` suffixes.
- Keeps the legacy automation planner aligned with the native app preview and import retry behavior.
