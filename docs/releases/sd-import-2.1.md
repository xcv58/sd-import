# SD Import 2.1

## Changes

- Simplifies Settings so it only stores default Photos and Videos destination folders plus stable app preferences.
- Moves per-import card source and shoot name choices out of Settings and keeps them in the Import flow.
- Aligns Settings rows and uses a softer `Unavailable` status for missing default destination folders.

No manual migration is needed.
