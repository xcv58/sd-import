# SD Import 1.22

- Sorts the import preview file list by action so conflicts, files that will copy, already-known files, unsupported sidecars, and excluded files are easier to scan.
- Adds quick preview filters for all files, files that will copy, skipped files, unsupported files, and conflicts.
- Keeps file order stable within each status group and improves the preview table spacing for longer status labels.
